import Placeholder from '../components/Placeholder'

// Lo que todavía no existe. Cada uno se reemplaza por su página real
// cuando le toca; el resto de los módulos ya están construidos.

export function Importar() {
  return (
    <Placeholder
      title="Importar datos"
      fase="Fase 6"
      descripcion="Importador de Excel/CSV con flujo seguro: subir → previsualizar → validar → detectar duplicados e inconsistencias → confirmar → importar. Nunca inserta sin revisión."
    />
  )
}

